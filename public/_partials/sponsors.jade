section.main.main-sponsors(role='region', aria-labelledby='sponsors')
  h2#sponsors.section-title(role='heading', aria-level='2') Sponsors

  //- if config.sponsorsAvailable
  //-   div.button-wrapper
  //-     a.button(href='/sponsorship.html') Become a sponsor

  each cat, key in public._data.sponsors
    if cat.length
      section.monty(class=key + '-monty')
        h3.sponsor-title(role='heading', aria-level='3')= capitalize(key) + ' Monty Sponsors'
        div.sponsors-wrapper
          each sponsor in cat
            section.sponsor(class='sponsor-' + key)
              a(href=sponsor.url, target="_blank", title=sponsor.name)
                img(src='/images/sponsors/' + sponsor.img, alt=sponsor.name)
  section.monty(class=small-monty)
    h3.sponsor-title(role='heading', aria-level='3')= 'Become a sponsor'
    a(href="/sponsorship.html") See our sponsorship pack & get in touch today